// Run this in MongoDB shell or Studio 3T to fix the index:

// 1. Drop the existing index on apiKeys.keyId
db.enhancedusers.dropIndex("apiKeys.keyId_1");

// 2. Create a new sparse unique index
db.enhancedusers.createIndex({ "apiKeys.keyId": 1 }, { unique: true, sparse: true });

// Alternative - if you need a one-liner for mongo shell:
// db.enhancedusers.dropIndex("apiKeys.keyId_1"); db.enhancedusers.createIndex({ "apiKeys.keyId": 1 }, { unique: true, sparse: true });
